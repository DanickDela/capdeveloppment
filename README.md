
  # Generate code for app

  This is a code bundle for Generate code for app. The original project is available at https://www.figma.com/design/S53lULrfoPPLFrNObwTsdM/Generate-code-for-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  